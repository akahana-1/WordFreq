#include <string.h>
#include <stdlib.h>

#define MAXLEN 1024

typedef struct word{
	//保存する文字列
	char* word;
	//文字列の出現回数
	int count;
}Word;

Word** arrayInsert(Word** ary, const char* _word){
	if(ary == NULL){

	}
}
